using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightShape : MonoBehaviour {
    public Shape shape;
	// Use this for initialization
	void Start () {
        //shape = new Line(this.transform.position, this.transform.position + this.transform.forward);
	}
	
	
}
